<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bno2.mapper.LoginMapper">

    <select id="login" resultType="com.example.bno2.dto.User">
        select
            pn,
            email,
            name,
            dept_code as deptCode,
            con,
            pos_code as posCode,
            state_code as stateCode
        from
            user
        where
            email = #{email}
        and
            pw = SHA1(#{password})
    </select>

    <select id="loginCount" resultType="int">
        select
            count(email)
        from
            user
        where
            email = #{email}
          and
            pw = SHA1(#{password})
    </select>

    <select id="getRecentLoginHistory" parameterType="int" resultType="java.sql.Timestamp">
        select
            login_out_dt
        from
            login_out_history
        where
            user_pn = #{userPn}
        and
            login_out = 1
        order by
            login_out_dt desc
        limit 1;
    </select>

    <insert id="addLoginHistory" parameterType="int">
        insert into
            login_out_history(user_pn, login_out_dt, login_out)
        values(#{userPn}, NOW() + INTERVAL + 9 HOUR, 1)
    </insert>

    <insert id="addLogoutHistory" parameterType="int">
        insert into
            login_out_history(user_pn, login_out_dt, login_out)
        values(#{userPn}, NOW() + INTERVAL + 9 HOUR, 2)
    </insert>

</mapper>